# print(batch_length)

# calculate the size of pad
# index = 0
# for sentence in open(filename_sorted):
#     max_length = 0
#     count_headline = len(sentence.split())
#     if max_length < count_headline:
#         max_length = count_headline
#     index += 1
#     print("%d %d" % (count_headline, max_length))
#     if index % batch_size == 0:
#         batch_length.append(max_length)
#         max_length = 0

